import * as permanceServices from '../services/permanenceServices.js';

export async function createPermanence(req, res) {
    /* obter entradas */
    /* delegar responsabilidades à camada de serviços 
       ex: await permanceServices.createPermanence(variáveis necessárias);
    */
    /* outras tarefas... */
    return res.sendStatus(201);
}

export async function getPermanence(req, res) {
    /* obter entradas */
    /* delegar responsabilidades à camada de serviços 
       ex: await permanceServices.getPermanence(variáveis necessárias); */
    /* outras tarefas... */
    return res.sendStatus(200);
}

export async function updatePermanence(req, res) {
    /* obter entradas */
    /* delegar responsabilidades à camada de serviços 
       ex: await permanceServices.updatePermanence(variáveis necessárias);*/
    /* outras tarefas... */
    return res.sendStatus(200);
}

export async function deletePermanence(req, res) {
    /* obter entradas */
    /* delegar responsabilidades à camada de serviços 
       ex: await permanceServices.deletePermanence(variáveis necessárias);*/
    /* outras tarefas... */
    return res.sendStatus(200);
}